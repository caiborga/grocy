spring:
    application:
        name: Grocy

    datasource:
        url: jdbc:postgresql://localhost:3300/grocy
        username: grocy_admin
        password: grocy

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
    mvc:
      servlet:
        path: /api
