spring:
    application:
        name: Grocy

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true

    mvc:
        servlet:
            path: /api

server:
    port: ${PORT:8000}
